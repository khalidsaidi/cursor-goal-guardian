{
  "schemaVersion": 2,
  "goal": "Ship the CSV export feature for the report table",
  "successCriteria": [
    { "id": "sc_1", "text": "Users can export the report table as CSV" },
    { "id": "sc_2", "text": "Export respects the currently active filters" },
    { "id": "sc_3", "text": "Unit tests cover the CSV serializer" }
  ],
  "constraints": ["No new dependencies", "No changes outside src/export/"]
}
